===== PyQCustomPlot ===== 

How to build:
1. Requires:
    - sip
    - PyQt5
    - QCustomPlot header, located at /usr/include
    - QCustomPlot lib, located at /usr/lib64

2. Build:
    - python configure.py
    - make
    - make install

3. Checks:
    - Run python interpritator and enter:
	import qcustomplot
	dir(qcustomplot)
